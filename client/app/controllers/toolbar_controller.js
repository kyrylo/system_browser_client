(function(global) {
  'use strict';

  var controller = function($scope, $rootScope, toolbar) {
  };

  global.app.controller('ToolbarController', [
    '$scope',
    '$rootScope',
    'toolbar',
    controller]);
})(window.global);
